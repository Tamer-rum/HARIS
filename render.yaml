services:
  - type: web
    name: haris-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python run_api.py
    envVars:
      - key: NAC_MODE
        value: fixture
      - key: ENABLE_CONTINUOUS_LOOP
        value: "false"
